# requirements.txt — compatible con Python 3.14

# ── LLM y agente ─────────────────────────────────────────────
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14

# ── LLM providers — instalá solo el que uses ─────────────────
langchain-ollama>=0.2.0        # provider: "ollama"  (local)
langchain-openai>=0.2.0        # provider: "openai"   (nube, GPT-4o)
# langchain-aws>=0.2.0         # provider: "bedrock"  → pip install langchain-aws boto3
# langchain-groq>=0.2.0        # provider: "groq"    → pip install langchain-groq

# ── Voz: STT (reemplaza openai-whisper) ──────────────────────
faster-whisper==1.1.0      # wheel disponible para Python 3.14

# ── Audio ─────────────────────────────────────────────────────
sounddevice==0.5.1
soundfile==0.12.1
numpy>=2.4.0

# ── Google APIs ──────────────────────────────────────────────
google-auth==2.28.1
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.120.0

# ── HTTP ─────────────────────────────────────────────────────
requests==2.31.0
websockets>=12.0          # GPT-4o Realtime API (modo realtime)

# ── Configuración ─────────────────────────────────────────────
pyyaml==6.0.1
python-dotenv>=1.0.0      # carga ../.env compartido con el backend Node.js
